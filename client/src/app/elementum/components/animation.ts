// facilitate animated entities

// X: render or entity
// displace X along path
// play series of renders
// play next frame for each draw call
// OR play frame according to time elapsed

// start with render and modify it

// take render as param and displace it

export class AnimationPath {
    constructor(isRelative: boolean) {
    }

    public draw() {
    }
}
